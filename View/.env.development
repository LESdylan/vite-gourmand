# ── Supabase / mini-BaaS connection ──────────────────────────────
# Kong gateway URL (direct in dev, through nginx/WAF in prod)
# Real keys are in .env.local (git-ignored). These are fallbacks.
VITE_SUPABASE_URL=http://localhost:8000
# VITE_SUPABASE_ANON_KEY intentionally omitted — .env.local provides it
